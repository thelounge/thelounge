// Actions


// Action Generators


// Reducer
const DEFAULT_STATE = {
	notification: true,
	notifyAllMessages: true
};

export default function (state = DEFAULT_STATE, action) {
	switch (action.type) {
	default:
		return state;
	}
}
